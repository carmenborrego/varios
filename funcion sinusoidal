
#se importan las librerias necesarias


import scipy as sp
import matplotlib.pyplot as plt

#crear array llamado x indicando lo siguiente: 0 que empiece en cero, 10 que vaya de diez en diez y 100 que llegue hasta 100
x = sp.linspace(0, 10, 100)

# se crea 'y' y se indicad quecada punto es el seno de cada elemento de x
y = sp.sin(x)

#pedimos que se grafique la figura resultante
plt.figure()

#graficador
plt.plot(x,y)

#pedimos que nos muestre las lineas para ver cuando la linea pasa por el cero
plt.grid(True)

#pedimos que muestre el resultado
plt.show()
